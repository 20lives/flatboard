{
  "name": "flatboard",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun run ./src/index.ts build",
    "build:dev": "bun run --watch ./src/index.ts build:dev",
    "build:stl": "bun run ./src/index.ts build:stl",
    "clean": "rm -rf ./dist/*.scad ./dist/*.stl",
    "list": "bun run ./src/index.ts list",
    "help": "bun run ./src/index.ts help",
    "format": "biome format --write .",
    "lint": "biome lint --write .",
    "check": "biome check --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.0",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "fp-ts": "^2.16.11",
    "scad-js": "^0.6.7"
  }
}
